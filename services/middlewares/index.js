const errorHandler = require('./errorHandler');
const verifySlipType = require('./verifySlipType');
const verifyIsAllNumber = require('./verifyIsAllNumber');

module.exports = {
  errorHandler,
  verifySlipType,
  verifyIsAllNumber,
};
